name: mysql1
version: 0.19.2
description: A library for connecting to and querying MySQL & MariaDB databases.
homepage: https://github.com/adamlofts/mysql1_dart

environment:
  sdk: ^3.0.0

dependencies:
  collection: ^1.17.0
  crypto: ^3.0.0
  logging: ^1.2.0
  pool: ^1.5.0

dev_dependencies:
  lints: ^2.1.0
  mocktail: ^0.3.0
  options_file:
    git: https://github.com/mnordine/optionsfile
  build_runner: ^2.4.0
